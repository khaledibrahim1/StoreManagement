<Window x:Class="StoreManagement.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:StoreManagement.Views"
        xmlns:viewmodels="clr-namespace:StoreManagement.ViewModels"
        Title="Store Management &amp; TPS" Height="768" Width="1024"
        WindowStartupLocation="CenterScreen" Background="#F5F6FA">
    <Window.DataContext>
        <viewmodels:MainViewModel/>
    </Window.DataContext>
    
    <Window.Resources>
        <views:ViewModelTypeToBooleanConverter x:Key="ViewModelTypeToBooleanConverter"/>
        
        <DataTemplate DataType="{x:Type viewmodels:DashboardViewModel}">
            <views:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:InventoryViewModel}">
            <views:InventoryView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:PosViewModel}">
            <views:PosView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:HistoryViewModel}">
            <views:HistoryView/>
        </DataTemplate>
        
        <!-- Sidebar Button Style -->
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="Margin" Value="0,3"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Active Indicator Bar -->
                                <Border x:Name="Indicator" Width="4" Height="16" Background="Transparent" CornerRadius="2" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                
                                <ContentPresenter Grid.Column="1" 
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Active state in template to color the Indicator -->
                            <Trigger Property="Tag" Value="True">
                                <Setter TargetName="Indicator" Property="Background" Value="#0D6EFD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <!-- Hover state -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F6FA"/>
                    <Setter Property="Foreground" Value="#0D6EFD"/>
                </Trigger>
                <!-- Active state -->
                <Trigger Property="Tag" Value="True">
                    <Setter Property="Background" Value="#EBF3FF"/>
                    <Setter Property="Foreground" Value="#0D6EFD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F4FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0">
            <StackPanel Margin="20">
                <!-- Fingoals style logo/header -->
                <StackPanel Orientation="Horizontal" Margin="0,20,0,40">
                    <Border Background="#0D6EFD" CornerRadius="8" Width="30" Height="30" Margin="0,0,10,0">
                        <TextBlock Text="S" Foreground="White" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="StoreManager" FontSize="20" FontWeight="Bold" Foreground="#333" VerticalAlignment="Center"/>
                </StackPanel>
                
                <TextBlock Text="MAIN MENU" Foreground="#AAA" FontSize="12" FontWeight="Bold" Margin="10,0,0,10"/>
                
                <Button Content="Dashboard" Command="{Binding ShowDashboardCommand}" Style="{StaticResource SidebarButtonStyle}"
                        Tag="{Binding CurrentViewModel, Converter={StaticResource ViewModelTypeToBooleanConverter}, ConverterParameter=DashboardViewModel}"/>
                
                <Button Content="POS Terminal" Command="{Binding ShowPosCommand}" Style="{StaticResource SidebarButtonStyle}"
                        Tag="{Binding CurrentViewModel, Converter={StaticResource ViewModelTypeToBooleanConverter}, ConverterParameter=PosViewModel}"/>
                
                <Button Content="Inventory Control" Command="{Binding ShowInventoryCommand}" Style="{StaticResource SidebarButtonStyle}"
                        Tag="{Binding CurrentViewModel, Converter={StaticResource ViewModelTypeToBooleanConverter}, ConverterParameter=InventoryViewModel}"/>
                
                <Button Content="Transaction History" Command="{Binding ShowHistoryCommand}" Style="{StaticResource SidebarButtonStyle}"
                        Tag="{Binding CurrentViewModel, Converter={StaticResource ViewModelTypeToBooleanConverter}, ConverterParameter=HistoryViewModel}"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <ContentControl Grid.Column="1" Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>
