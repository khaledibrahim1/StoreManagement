<UserControl x:Class="StoreManagement.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:StoreManagement.Views"
             Background="#F5F6FA">
    <UserControl.Resources>
        <local:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- Transactions List -->
        <Border Background="White" CornerRadius="15" Padding="20" Margin="0,0,20,0">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.05" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Transaction History" FontSize="20" FontWeight="Bold" Foreground="#333" Margin="0,0,0,15"/>
                
                <DataGrid Grid.Row="1" ItemsSource="{Binding Transactions}" SelectedItem="{Binding SelectedTransaction}" 
                          AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                          Background="Transparent" RowBackground="White" AlternatingRowBackground="#F9F9F9"
                          BorderThickness="0" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E0E0E0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Trans ID" Binding="{Binding Id}" Width="80"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="150"/>
                        <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Transaction Details -->
        <Border Grid.Column="1" Background="#111" CornerRadius="15" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Transaction Details" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>
                
                <StackPanel Grid.Row="1" Margin="0,0,0,20" Visibility="{Binding SelectedTransaction, Converter={StaticResource NullToVisibilityConverter}}">
                    <TextBlock Text="{Binding SelectedTransaction.Id, StringFormat='Transaction #{0}'}" Foreground="#0D6EFD" FontSize="16" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding SelectedTransaction.Date, StringFormat='Date: {0:yyyy-MM-dd HH:mm}'}" Foreground="#AAA" FontSize="14" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding SelectedTransaction.Subtotal, StringFormat='Subtotal: {0:C}'}" Foreground="#AAA" FontSize="14"/>
                    <TextBlock Text="{Binding SelectedTransaction.Tax, StringFormat='Tax: {0:C}'}" Foreground="#AAA" FontSize="14"/>
                    <TextBlock Text="{Binding SelectedTransaction.Total, StringFormat='Total: {0:C}'}" Foreground="White" FontWeight="Bold" FontSize="16" Margin="0,5,0,0"/>
                </StackPanel>

                <DataGrid Grid.Row="2" ItemsSource="{Binding SelectedTransactionItems}" 
                          AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                          Background="Transparent" RowBackground="#222" AlternatingRowBackground="#1A1A1A"
                          Foreground="White"
                          BorderThickness="0" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#333">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" Width="*"/>
                        <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="50"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
